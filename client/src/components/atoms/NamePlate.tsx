import App from "../../App";

const NamePlate = (props: number) => {
  const userId = props;
  <App />;

  return (
    <>
      <ul>　ようこそ、{userId ? userId : "undefined"}さん</ul>
    </>
  );
};
export default NamePlate;
